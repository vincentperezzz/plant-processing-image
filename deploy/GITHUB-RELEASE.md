# Plant Health kiosk — GitHub release

**Tag:** `v2026.08.19`  
**Asset:** `plant-health-kiosk-2026.08.19.zip`

Attach only that zip. The zip root is `plant-health-kiosk/` (README, INSTALL, grader, Pi wheels).

Paste this as the release body:

```
Windows PC simulation and Raspberry Pi 4B kiosk. Same MobileNet grader (best.pt).

Windows: unzip → deploy\setup-pc.ps1 → deploy\run-kiosk.ps1 (needs pip once).
Raspberry Pi: unzip → bash deploy/install-pi.sh (offline) → bash deploy/run-kiosk.sh

See INSTALL.md inside the zip.
```
